export enum SettingType {
  separators_input,
  select,
  multiselect,
  binary,
  button,
  root_dirs,
  free_number_input,
  locked_number_input,
  license,

  // custom components 👇
  quick_actions,
  profile,
  accounts,
  pairing,
  about,
  streaming_quality,
  backup,
  secretinput,
}
