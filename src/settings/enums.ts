export enum SettingType {
  separators_input,
  select,
  multiselect,
  binary,
  button,
  root_dirs,
  locked_number_input,
  
  // custom components 👇
  quick_actions,
  profile,
  accounts,
  pairing,
  about
}
